export enum OrderStatus {
  DRAFT = 'draft',
  PLACED = 'placed',
  CONFIRMED = 'confirmed',
  SEPARATED = 'separated',
  SHIPPED = 'shipped',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
  REJECTED = 'rejected',
}
